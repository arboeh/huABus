#include <tunables/global>

profile huawei_solar_modbus_mqtt flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  # Capabilities
  capability,
  file,
  signal (send) set=(kill,term,int,hup,cont),

  # Network
  network inet stream,
  network inet dgram,
  network inet6 stream,
  network inet6 dgram,
  network tcp,
  network udp,

  # S6-Overlay
  /init ix,
  /bin/** ix,
  /usr/bin/** ix,
  /sbin/** ix,
  /usr/sbin/** ix,
  /lib/** ix,
  /usr/lib/** ix,
  /run/{s6,s6-rc*,service}/** ix,
  /package/** ix,
  /command/** ix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /run/{,**} rwk,
  /dev/tty rw,
  /dev/null rw,
  /dev/zero rw,

  # Run script
  /run.sh rix,

  # Bashio
  /usr/lib/bashio/** ix,
  /tmp/** rwk,

  # Data directory
  /data/** rw,

  # Application
  /app/** r,
  /usr/bin/python3* ix,

  # System files
  /etc/** r,
  /proc/** r,
  /sys/** r,

  # Deny dangerous writes
  deny /proc/sys/kernel/** w,
  deny /sys/firmware/** w,
}
